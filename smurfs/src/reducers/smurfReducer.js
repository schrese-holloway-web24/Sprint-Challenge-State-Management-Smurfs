const initialState = {
    smurf: {},
    isFetchcing: false,
    error: '',
    newSmurf: [
        {

        }
    ]
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default: 
            return state;
    }
}